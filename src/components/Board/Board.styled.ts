import styled from "styled-components";

export const StyledBoard = styled.div`
  margin: 0 auto;
  display: inline-grid;
  grid-template-columns: repeat(8, min-content);
  grid-template-columns: repeat(8, min-content);
  border: solid 20px #4d230d;
  box-shadow: 0rem 1rem 2rem rgba(0, 0, 0, 0.7);
`;
